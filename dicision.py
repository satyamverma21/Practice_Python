#lets find out how we count elements in lists and use them with if statements
#we use print(len(users)) to get  number of elements
users = ["satyam" , "sajitya" , "govind"]
number_of_user = len(users)
print(number_of_user)
#we can use element number to create conditions EXAMPLE-----
if len(users)>0:
    print()